Web API and API gateway


// Added studentService as submodule 


1. git add submodule {Repo URL} {Folder Structure}    ---- Folder Structure means where I want to add submodule in your current service location
2. git module init
3. git module update
4. git add .gitmodules {Folder structure}
5. git commit -m "commit"

6. git push 
